//Write a function in JavaScript that takes an array of numbers as input and returns the sum of all the numbers in the array.

//Write a function in JavaScript that takes a string as input and returns the reverse of the string. For example, if the input is "hello", the output should be "olleh".

//Write a function in JavaScript that takes a number as input and returns true if the number is a prime number, and false otherwise.

//Write a function in JavaScript that takes an array of integers as input and returns a new array containing only the unique elements from the input array. The order of elements in the output array should remain the same as in the input array.

// Write a function in JavaScript that takes a string as input and returns true if the string is a palindrome, and false otherwise. A palindrome is a word, phrase, number, or other sequence of characters that reads the same forward and backward. Ignore spaces, punctuation, and capitalization when determining if a string is a palindrome.

//Write a function in JavaScript that takes an array of integers as input and returns the maximum difference between any two numbers in the array. The maximum difference is calculated by subtracting the smallest number from the largest number in the array.

//Write a function in JavaScript that takes a string as input and returns the count of each unique character in the string, ignoring spaces and considering case sensitivity.Output: { h: 1, e: 1, l: 3, o: 2, w: 1, r: 1, d: 1 }

//Write a function in JavaScript that takes a string as input and returns true if the string is a pangram, and false otherwise. A pangram is a sentence containing every letter of the alphabet at least once.

//Write a function in JavaScript that takes a string as input and returns the number of vowels in the string. Consider 'a', 'e', 'i', 'o', and 'u' as vowels (case-insensitive).

//Write a function in JavaScript that takes a string as input and returns the number of words in the string. Assume that words are separated by spaces.
